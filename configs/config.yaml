server:
  listen_addr: ":8080"
  read_timeout: 30s
  write_timeout: 30s
  max_message_size: 10485760  # 10MB
  max_connections: 1000

metrics:
  enabled: true
  port: 8081

log:
  level: info
  pretty: false
  output: stdout
